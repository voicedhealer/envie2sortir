# Dockerfile pour le développement
FROM node:20-alpine

WORKDIR /app

# Installer les dépendances système nécessaires pour Prisma et SQLite
RUN apk add --no-cache openssl libc6-compat sqlite

# Copier les fichiers de dépendances
COPY package*.json ./

# Installer les dépendances (y compris devDependencies)
RUN npm ci

# Copier le reste du code
COPY . .

# Générer le client Prisma
RUN npx prisma generate

EXPOSE 3000

# Le volume montera le code source pour le hot-reload
CMD ["npm", "run", "dev"]

